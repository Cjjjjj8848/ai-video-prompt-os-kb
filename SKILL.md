---
name: ai-video-prompt-os
description: AI 视频 Prompt 操作系统（V6.1.0 质感锚点版）。当用户输入 /蒸馏、/学习、/生成、/优化、/同步 等斜杠命令，或需要蒸馏优秀视频提示词、沉淀DNA知识库、生成视频分镜提示词、拆解爆款视频结构、制作短视频/广告/电影分镜、优化已有Prompt、生成人物四视图/角色设定表、人像生图时使用。核心能力：把优秀Prompt蒸馏成八维DNA（镜头/运镜/转场/动作链/时间结构/视觉规则+声音+台词），每条带 app 应用端标记（video/image/both），评分去重后入库。V6.1 升级：四档分流（L1硬广功能片/L2剧情流广告/L3微电影/L4系列短剧）+ 分镜决策引擎（场景锚点地图/空间物理矩阵/重技法频率上限/情绪曲线+钩子五级/反廉价化三层/衔接四件套+音桥视桥）+ 逐镜头关键帧卡片（不再强制九宫格）+ 质感锚点系统（六类锚点/光源动机铁律/五层色彩/纵深主层机制/商业片防血线皮肤写法）+ 按档质检门控。含 V5 融合：镜头语言速查表+多模态引用语法+12场景模板库+6条视频质量门控+中英双文强制输出+人物四视图双场景模板。支持GitHub知识库同步。
---

# AI Video Prompt OS V6

一套「Prompt DNA 蒸馏 + 可进化知识库 + 分镜决策引擎 + 生成」的视频提示词系统。

**核心定位**：SKILL.md 只装"怎么用"的规则，永远不变；真正增长的是 `knowledge/` 知识库（每 /学习 一次就新增 DNA）。

**V6 核心变化**：从"强制九宫格"升级为"四档分流 + 逐镜头关键帧卡片"。一套分镜引擎（storyboard_engine.md）同时支撑硬广/剧情流广告/微电影/短剧，按档（type_routing.md）开关剧本层模块。

## 五条核心命令

| 命令 | 作用 | 数据流 |
|---|---|---|
| `/蒸馏` | 分析优秀视频/生图 Prompt，提取**八维 DNA**（镜头/运镜/转场/动作/时间/视觉+🔴声音+🔴台词），每条标注 app 应用端（video/image/both） | Prompt → DNA 草案 |
| `/学习` | 蒸馏 → 评分 → 去重 → 合并 → 写回知识库（本地或 GitHub） | DNA → knowledge/ |
| `/生成` | **阶段0判档(L1/L2/L3/L4)** → 先按 app 应用端过滤 → 检索 DNA → 场景锚点地图 → 逐镜头关键帧卡片 → 按档质检 → 视频 Prompt | knowledge/ → 新 Prompt |
| `/优化` | 检查已有 Prompt → 对照 DNA 修正镜头/动作/转场/时间轴/模型限制 | 旧 Prompt → 新 Prompt |
| `/同步` | 拉取最新知识库（GitHub 或本地），建立当前会话索引 | 仓库 → 会话 |

## 知识库结构

```
knowledge/
├── DNA.json              # 核心可复用 DNA（结构化条目，可被检索）
├── DNA_schema.json       # DNA 条目字段规范（评分/去重/合并规则）
├── camera/               # 镜头语言库（景别/机位/构图）
├── shot/                 # 分镜结构库（节奏/多场景）
├── movement/             # 运镜库（推拉摇移/POV/环绕）
├── transition/           # 转场库
├── action/               # 动作链库（产品动作/人物动作/微观动作）
├── lighting/             # 灯光与氛围库
├── audio/                # 声音设计库（BGM/音效/ASMR）
├── commercial/           # 商业广告结构库
├── dialogue/             # 台词与口播库（对白结构/口播类型/多语言）
├── cinema/               # 电影导演手法库
└── model_rules/          # 各模型专用规则（Seedance/产品一致性/镜头语言速查表/多模态引用/画质一致性锁定/🔴V6 type_routing四档路由/🔴V6 storyboard_engine分镜决策引擎/🔴V6.1 quality_anchors质感锚点系统）
```

## 运行模式

- **模式 A 本地库**：直接读取本项目 `knowledge/` 与 `templates/`，无需联网。
- **模式 B GitHub 增强**：若环境具备 GitHub 工具，`/同步` 时拉取远程最新知识库；`/学习` 时把新 DNA 写回并自动 commit。GitHub 不可用则回退模式 A。

## /蒸馏 流程（八维 + 应用端适配）

输入：一个优秀视频/生图 Prompt（可附视频/分镜图）。

输出八个维度的 DNA 草案（图像端条目自动适配）：
1. **镜头 DNA**：景别、机位、构图、焦段（生图端=构图/景别/解剖锚点）
2. **运镜 DNA**：镜头运动方式（生图端=**光学质感**：720p/噪点/失焦/颗粒）
3. **转场 DNA**：段间过渡方式（生图端=N/A）
4. **动作链 DNA**：画面内的主体动作序列（生图端=**微表情/高势能瞬间**）
5. **时间结构**：时长分配、节奏、情绪曲线（生图端=N/A）
6. **视觉规则**：色调、光影、材质、风格关键词（生图端=光影/肤质/肌理/风格）
7. **声音 DNA**（🔴 新增）：音效层级、精确时间轴、多声轨、BGM 曲线、人声群像、空间回响、口播节奏
8. **台词 DNA**（🔴 新增）：对白结构（硬广三段式/种草自然对白）、口播类型（倒计时仪式/开场白/无CTA）、多语言规则、文案风格

**每条 DNA 草案必须标注应用端**：`app: video / image / both`——生图端条目（如四视图、720p低保真）不套用运镜/转场/时间维度，标 N/A。

## /学习 流程（入库三步）

1. **评分**：按 `learning/scoring_rules.md` 打分（新颖性/复用性/清晰度/商业价值），低于阈值进 `pending_updates.md` 待审，不直接入库。
2. **去重**：与 DNA.json 及现有分类库比对，重复或低差异条目丢弃。
3. **合并/写回**：新 DNA 编号（DNA #N+1）写入对应库；模式 B 下同步 commit 到 GitHub。

## 🔴 升级后收尾 SOP（每次 /学习 或框架升级后强制执行）

> 换环境/换使用者时无法判断是否最新，所以每次升级后必须走完以下 6 步，不靠记忆、不跳步。

1. **全量残留扫描**：在整个 skill 目录 grep 旧版本关键词（旧版本号/旧维度数/旧标签，如 `6维`、`V5 融合版`、旧版本号），命中即改，直到 0 残留（learning_log 历史记录除外）。
2. **三处口径核对**：SKILL.md（frontmatter 描述 + 一级标题 + 五条命令表 + 知识库结构树 + 智能路由表）、README.md、各模板/规范文件的版本标签必须全部对齐当前版本号。
3. **数字与实际一致**：DNA 条数、version、门控条数、模板数，必须与实际文件回读核对一致，禁止凭记忆写数字。
4. **阶段编号连续**：/生成 各阶段步骤编号无重复无跳号。
5. **三处同步**：豆包 skill 副本 → GitHub（commit+push，注意本地代理）→ 桌面打包文件夹 + ZIP 重建，逐文件比对一致（差异 0）。
6. **交付**：present_files 交付最新打包，并附改动清单（改了哪些文件、commit 号、版本号、DNA 条数）。

## /生成 流程（最高优先级：分阶段确认门控）

> ⚠️ **最高优先级工作流规则**：`/生成` 必须分阶段推进，**禁止跳过分镜确认直接给最终 Prompt**。每阶段完成后必须等用户确认或修改，再进入下一阶段。模型不得自行合并阶段或跳过确认。

### 智能路由 + 判档（V6）

先判档（L1/L2/L3/L4，详见 `knowledge/model_rules/type_routing.md`），再按任务类型调模板：

| 任务 | 自动判档 | 自动调用 |
|---|---|---|
| 抖音/TikTok 带货硬广、产品展示、参数对比、试色、开箱 | **L1** | 千川/TikTok 模板 + 简化情绪曲线 |
| 有剧情的广告（充电宝便利店小丑、圣诞投影灯家庭惊喜式微剧情） | **L2** | 叙事模板 + 完整情绪曲线 + 钩子五级 + CTA 尾 |
| 品牌故事/微电影/1分钟以上单篇叙事 | **L3** | 电影导演模板 + 完整六层人物 + 余韵七法 |
| 多集连续剧/短剧 | **L4** | L3 + 每集尾钩 + 不注水检测 |
| 人物四视图/角色设定表（character turnaround / model sheet） | 不判档，走阶段2.5分支 | character_turnaround 模板 + DNA-050/051/052 |
| 素人写实人像/低保真人像（GPT Image / DALL-E 3） | 不判档，生图任务 | DNA-061~065 |
| 用户显式指定档（如 `/生成 L2`） | 强制使用 | — |
| 不确定 | 自动判 + 请用户确认 | — |

**模板负责"结构"，DNA 负责"能力"，当前商品负责"具体内容"。** 最终输出 = 模板 + 已学习 DNA + 当前产品需求，不是简单复制模板。

### 阶段 0：判档 + 参数收集（先应用端过滤）

1. **判档**：按产品占比/情绪深度/是否有CTA/时长判 L1-L4，输出档位并请用户确认（"这看起来是 L2 剧情流广告，对吗？"）。用户可改档。
2. **🔴 先按应用端过滤 DNA**：生图任务只看 `app: image/both` 条目，视频任务只看 `app: video/both` 条目。
3. **再按相关维度检索**（如"磁吸充电宝广告"→ commercial + action + camera + movement + dialogue + audio），不整库读入。
4. 按需反问补齐关键参数（产品/人群/时长/平台/风格/素材/模型）。

### 阶段 1：剧本设计 + 场景锚点地图 + 情绪曲线（必须等用户确认）

> 替代旧版"分镜草案"。这是**购买欲生成的关键一步**——先想清楚卖给谁、戳哪个日常痛，再建镜头，不要先拍画面再硬塞卖点。

5. **🔴 购买欲五步法（DNA-072，L2/L3 剧情流广告强制）**：
   - 用户说出产品后，先定**观众的日常痛点**（不是演员的痛点，是目标用户自己每周都会遇到的麻烦）
   - 按五拍排时间轴：①钩子(0-3s别划走) → ②痛点具象化(3-8s"这说的就是我") → ③产品解法登场(高潮点) → ④爽点证明(视觉化的爽，不念参数) → ⑤拥有感+CTA
   - 自检三因子：观众也有过这麻烦？产品用得爽？我也用得上？缺哪个补哪个
   - 三个目标对应：钩子强=①，有看完欲=②③，看完想买=④⑤
6. **建场景锚点地图**（详见 `storyboard_engine.md §1`）：每个场景输出三类锚点——光源锚点/家具道具锚点/通道锚点，标注空间类型（S1-S4/O1/O2/X）。
7. **画情绪曲线 + 钩子**（§4）：
   - L1：简化版（钩子→痛点→方案→CTA）
   - L2/L3/L4：标情绪节点（类型+强度1-10+持续度），前3s钩子≥L3
8. **定镜头数**：不强制 9 个。L1 3-6镜、L2 5-10镜、L3/L4 视时长。
9. **停止，等用户确认剧本（痛点+五拍）+ 锚点地图 + 情绪曲线 + 镜头数。** 用户未确认前不进阶段2。

### 阶段 2：逐镜头关键帧卡片（用户确认后）

> 替代旧版"强制九宫格"。每镜一张卡片，镜头数随剧情走，不拼 3×3 格。

9. 按 `storyboard_engine.md §7` 的卡片模板逐镜输出，每镜必含：
   - 空间类型（§2 物理可行性自检：12 条物理不可能镜头出现即废）
   - 景别 + 机位 + 运镜（重技法过 §3 频率上限：Dolly Zoom≤1/Orbit≤3/长镜头≤2/荷兰角≤4/升降≤2/POV≤3）
   - **锚点绑定**：人物从{通道锚点}到{家具锚点}，光从{光源锚点}来；禁止"左边/旁边"无参照方位词
   - **高势能瞬间动态**：正在进行的动作（"正啪地吸到手机背面"），禁止静态摆拍
   - 台词/声音/转场/下游映射标签
10. **跨段衔接表**（§6）：逐段打勾（人物位置/光线/色调/情绪/关键点/渲染技术）。
11. **🔴 关键帧图提示词规范**（详见 `knowledge/model_rules/storyboard_prompt_spec.md`，V6 适配）：
    - 每镜单独出一张图提示词（不拼格），八要素仍保留：景别+构图+主体+高势能瞬间+场景+光源方向+材质景深+视觉预设
    - 全局硬锁定：@图片1 主体外观锁定 + 禁止改设计
    - 六种风格预设统一
    - 中英双文分开输出
12. **可选九宫格模式**：用户明确要 3×3 拼格预览时，才拼九宫格并加格间防渗透约束；默认不拼。
13. 输出逐镜头关键帧提示词（中英分开）+ 衔接表 + 重技法频率自检表。

### 阶段 2.5：人物四视图分支（角色设定任务专用）

> 🔴 当任务类型为「人物四视图 / 角色设定表 / character turnaround / model sheet」时，不走九宫格分镜流程，改走本分支。详见 `templates/nanobanana/character_turnaround.md` + `knowledge/model_rules/character_turnaround_spec.md`。

- **有参考图**：场景 A → `--cref [参考图URL] --cw 100` 强锁脸，严格不改参考图任何画面，只换视角
- **无参考图**：场景 B → 文字死写全部识别特征（年龄/肤色/发型/五官/痣/配饰/服装/鞋/比例），强调 "All four panels must show the SAME person"
- 布局：单张 16:9，四等宽竖栏 = 脸部胸像特写 / 全身正面 / 全身侧面 / 全身背面（1栏脸部:3栏全身）
- 输出中英双文版 + 过四视图专用 4 条门控（character_turnaround_spec.md 第6节），附自检表
- 四视图图生成后可作为后续九宫格/视频的 `@图片` 人物参考

### 阶段 3：Seedance 视频 Prompt（关键帧图生成后）

12. 基于已生成的逐镜头关键帧图 + 产品参考图，生成 Seedance 视频 Prompt。
13. 视频 Prompt = 关键帧要素 **+ 追加**：时间轴 + 镜头运动 + 转场 + 动作链 + 连续性 + 物理交互 + 对白 + 音效。
14. **🔴 强制包含画质与一致性锁定**（详见 `knowledge/model_rules/quality_and_consistency.md` + `storyboard_engine.md §5 反廉价化三层`）：每条视频 Prompt 必须包含【画质要求】+【一致性锁定】+【禁忌】三段，缺则不合格。
15. **🔴 P0 内聚化格式**（详见 `camera_language_cheatsheet.md`）：声音内嵌到分镜下方（`└─ 声音：`），多场景切换处标转场（`└─ 转场：`），禁止声音集中放在时间轴后独立大块。
16. **🔴 P2 质量门控**（详见 `quality_gate_checklist.md`）：逐条过 6 条（结构/多模态/镜头语言/一致性/商业合规/内聚化）。
17. **🔴 V6 按档质检门控**（见 `type_routing.md` 每档门控）：
    - L1：产品占比>70%、前3s钩≥L3、卖点≤3、CTA明确
    - L2：单情绪点、每镜必要性三问、产品在高潮点引入、CTA尾独立3-5s、轻量反俗套三问
    - L3：完整情绪曲线、人物want+need、余韵≥2招、反俗套十查、非人物镜头30-50%
    - L4：L3+每集尾钩≥L3+不注水+钩子不重复
18. 输出可直接投喂模型的视频提示词 + @媒体引用 + 出片设置 + **质量门控自检表**（P2六条 + 按档门控）。

### 两种 Prompt 的区分（不可混淆）

| 类型 | 用途 | 核心要素 | 不包含 |
|---|---|---|---|
| **关键帧生图 Prompt**（V6 默认逐镜头单张；可选拼九宫格） | Gemini / Nano Banana 出单张关键帧 | 景别+构图+主体+高势能瞬间+场景+风格+锚点绑定 | 时间轴、运镜、转场、对白、音效 |
| **Seedance 视频 Prompt** | Seedance 生成视频 | 关键帧要素 + 时间轴+运镜+转场+动作链+连续性+对白+音效 | — |

> 禁止把两种 Prompt 混成一个。生图 Prompt 只负责画面，视频 Prompt 才负责时间与运动。

## /优化 流程

对已有 Prompt 做局部修正：指哪改哪，默认"其余不变"，不推倒重来。支持改时长/改场景/改元素/改对白/改风格。

## 硬性原则

- **🔴 最高优先级**：`/生成` 必须分阶段推进，禁止跳过分镜确认直接给最终 Prompt（见 `/生成 流程`）。
- 全程使用用户语言；台词用目标市场语言并附中文释义。
- **产品外观/颜色/logo 以用户图为唯一事实**，提示词不得改设计（见 model_rules/product_consistency.md）。
- 跨境与国内打法不混用：用户做哪个市场就用哪套表达与平台规则。
- **具体工具名/入口不写死**（模型迭代快），只保留"简单品用轻量模型、高难度镜头用旗舰模型"的分层策略。
- 每阶段产物可直接复制使用，不给半成品骨架。
- **🔴 中英双文强制输出**：所有提示词（分镜图提示词 + 视频提示词）必须输出中英双文版，先中文后英文，**分开输出不混排**，每版独立完整块方便直接复制；英文版严格过语言一致性门控（正文无中文字符残留，段落标题可保留中文标记如【Style】）。
- 先验证再放量：先出 2-3 条测方向，数据可行再批量。
