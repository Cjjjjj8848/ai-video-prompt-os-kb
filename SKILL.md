---
name: ai-video-prompt-os
description: AI 视频 Prompt 操作系统（V5 融合版）。当用户输入 /蒸馏、/学习、/生成、/优化、/同步 等斜杠命令，或需要蒸馏优秀视频提示词、沉淀DNA知识库、生成视频分镜提示词、拆解爆款视频结构、制作短视频/广告/电影分镜、优化已有Prompt时使用。核心能力：把优秀Prompt蒸馏成6维DNA（镜头/运镜/转场/动作链/时间结构/视觉规则），评分去重后入库，再按新任务检索DNA+套模板分阶段产出九宫格生图Prompt与Seedance视频Prompt。含 V5 融合升级：镜头语言速查表+多模态引用语法+12场景模板库+6条视频质量门控+5条分镜图门控+中英双文强制输出。支持GitHub知识库同步。
---

# AI Video Prompt OS V5

一套「Prompt DNA 蒸馏 + 可进化知识库 + 生成引擎」的视频提示词系统。

**核心定位**：SKILL.md 只装"怎么用"的规则，永远不变；真正增长的是 `knowledge/` 知识库（每 /学习 一次就新增 DNA）。

## 五条核心命令

| 命令 | 作用 | 数据流 |
|---|---|---|
| `/蒸馏` | 分析一个优秀视频 Prompt，提取镜头/运镜/转场/动作/时间结构/视觉规则 DNA | Prompt → DNA 草案 |
| `/学习` | 蒸馏 → 评分 → 去重 → 合并 → 写回知识库（本地或 GitHub） | DNA → knowledge/ |
| `/生成` | 读取已有 DNA → 按新任务检索 → 分阶段产出分镜→生图→视频 Prompt | knowledge/ → 新 Prompt |
| `/优化` | 检查已有 Prompt → 对照 DNA 修正镜头/动作/转场/时间轴/模型限制 | 旧 Prompt → 新 Prompt |
| `/同步` | 拉取最新知识库（GitHub 或本地），建立当前会话索引 | 仓库 → 会话 |

## 知识库结构

```
knowledge/
├── DNA.json              # 核心可复用 DNA（结构化条目，可被检索）
├── DNA_schema.json       # DNA 条目字段规范（评分/去重/合并规则）
├── camera/               # 镜头语言库（景别/机位/构图）
├── shot/                 # 分镜结构库（节奏/多场景）
├── movement/             # 运镜库（推拉摇移/POV/环绕）
├── transition/           # 转场库
├── action/               # 动作链库（产品动作/人物动作/微观动作）
├── lighting/             # 灯光与氛围库
├── audio/                # 声音设计库（BGM/音效/ASMR）
├── commercial/           # 商业广告结构库
├── cinema/               # 电影导演手法库
└── model_rules/          # 各模型专用规则（Seedance/九宫格/产品一致性/镜头语言速查表/多模态引用/画质一致性锁定）
```

## 运行模式

- **模式 A 本地库**：直接读取本项目 `knowledge/` 与 `templates/`，无需联网。
- **模式 B GitHub 增强**：若环境具备 GitHub 工具，`/同步` 时拉取远程最新知识库；`/学习` 时把新 DNA 写回并自动 commit。GitHub 不可用则回退模式 A。

## /蒸馏 流程

输入：一个优秀视频 Prompt（可附视频/分镜图）。

输出六个维度的 DNA 草案：
1. **镜头 DNA**：景别、机位、构图、焦段
2. **运镜 DNA**：镜头运动方式
3. **转场 DNA**：段间过渡方式
4. **动作链 DNA**：画面内的主体动作序列
5. **时间结构**：时长分配、节奏、情绪曲线
6. **视觉规则**：色调、光影、材质、风格关键词

## /学习 流程（入库三步）

1. **评分**：按 `learning/scoring_rules.md` 打分（新颖性/复用性/清晰度/商业价值），低于阈值进 `pending_updates.md` 待审，不直接入库。
2. **去重**：与 DNA.json 及现有分类库比对，重复或低差异条目丢弃。
3. **合并/写回**：新 DNA 编号（DNA #N+1）写入对应库；模式 B 下同步 commit 到 GitHub。

## /生成 流程（最高优先级：分阶段确认门控）

> ⚠️ **最高优先级工作流规则**：`/生成` 必须分阶段推进，**禁止跳过分镜确认直接给最终 Prompt**。每阶段完成后必须等用户确认或修改，再进入下一阶段。模型不得自行合并阶段或跳过确认。

### 智能路由

先判断任务类型，自动调用对应模板 + DNA：

| 任务 | 自动调用 |
|---|---|
| 抖音 / TikTok 带货 | 千川 / TikTok 模板 |
| 产品广告 | 商业广告模板（scene_templates 模板12） |
| Seedance 视频 | Seedance 模板 + 对应 DNA + scene_templates 场景模板 |
| 电影短片 | 电影导演模板 |
| 剧情短视频 | 叙事模板（scene_templates 模板8） |
| 音乐卡点 | scene_templates 模板1 |
| 视频延长/续拍 | scene_templates 模板2 |
| 空间漫游/参观 | scene_templates 模板3 |
| 口播视频 | scene_templates 模板4 |
| 伪纪录片/Vlog | scene_templates 模板5 |
| 产品动效/APP展示 | scene_templates 模板6 |
| 角色动作/打斗 | scene_templates 模板7 |
| 风景旅拍 | scene_templates 模板9 |
| 长镜头追踪 | scene_templates 模板10 |
| 战争/宏大场景 | scene_templates 模板11 |
| 不确定 | AI 自动判断 |
| 用户指定模板（如 `/生成 千川`） | 强制使用指定模板 |

**模板负责"结构"，DNA 负责"能力"，当前商品负责"具体内容"。** 最终输出 = 模板 + 已学习 DNA + 当前产品需求，不是简单复制模板。

### 阶段 0：参数收集

1. 解析任务类型（商业广告 / 电影分镜 / 短视频 / 长视频…）。
2. **只检索相关 DNA**（如"磁吸充电宝广告"→ commercial + action + camera + movement 相关条目），不整库读入。
3. 套用 `templates/` 对应模板。
4. 按需反问补齐关键参数（产品/人群/时长/平台/风格/素材/模型）。

### 阶段 1：分镜草案（必须等用户确认）

5. 输出 N 镜头分镜表，每镜必须包含：**景别、运镜、主体动态、画面内容、情绪氛围**。
6. **停止，等用户确认或修改分镜。** 用户未明确确认前，不得进入阶段 2。用户提出修改则修改后再次等待确认。

### 阶段 2：九宫格生图 Prompt（用户确认分镜后）

7. 将确认后的分镜转为九宫格生图 Prompt（3×3，分镜故事板模式，每格对应一个分镜）。
8. **🔴 V5 分镜图升级规范**（详见 `knowledge/model_rules/storyboard_prompt_spec.md`）：
   - **八要素**（每格必须包含）：景别 + 构图 + 主体 + **高势能瞬间动态** + 场景 + **光源方向** + **材质/景深** + 视觉预设
   - **🔴 高势能瞬间规则**：动态必须包含未完成的动势（"瞬间""中途""腾空""炸裂"），禁止静态描述，为下游视频模型预留运动势能
   - **全局硬锁定模块**：任务 + 主体锁定（@图片1+外观描述+禁止改动设计）+ 全局风格（6大预设之一）+ 交付约束
   - **🔴 格间防渗透约束**（强制）：交付约束中必须加入 `3x3 Grid, separated by clean thin grid lines, distinct independent frames, no element bleeding across borders`
   - **🔴 下游视频映射标签**（故事板模式强制）：每格末尾标注 `➔ [映射: 视频X-Ys首帧/材质参考/场景参考/备用]`
   - **六种风格预设**：商业广告/电影感/日系清新/科技感/复古胶片/极简白底，所有格统一使用同一个预设
9. **🔴 分镜图专用 5 条质量门控**（详见 storyboard_prompt_spec.md 第6节）：输出前必须逐条过：
   - 门控1 结构完整性（全局4段+每格八要素+防渗透+映射标签）
   - 门控2 主体一致性（外观锁定+每格重复+颜色固定）
   - 门控3 光影与景深统一（风格预设统一+光源方向+景深分层+高光控制）
   - 门控4 多模态引用合规（编号连续+用途标注+主体参考）
   - 门控5 商业禁忌过滤（无文字水印/畸变/形变/跨格渗色/真实品牌名+高势能动态）
10. 输出九宫格生图 Prompt + 门控自检表。用途：Gemini / Nano Banana → 生成 3×3 九宫格分镜图。

### 阶段 3：Seedance 视频 Prompt（九宫格图生成后）

11. 基于已生成的九宫格分镜图 + 产品参考图，生成 Seedance 视频 Prompt。
12. 视频 Prompt = 九宫格生图要素 **+ 追加**：时间轴 + 镜头运动 + 转场 + 动作链 + 连续性 + 物理交互 + 对白 + 音效。
13. **🔴 强制包含画质与一致性锁定**（详见 `knowledge/model_rules/quality_and_consistency.md`）：每条视频 Prompt 必须包含【画质要求】+【一致性锁定】+【禁忌】三个段落，缺少视为不合格。
14. **🔴 P0 内聚化格式**（详见 `knowledge/model_rules/camera_language_cheatsheet.md`）：声音必须内嵌到分镜下方（`└─ 声音：`），多场景切换处必须标注转场方式（`└─ 转场：`），禁止声音集中放在时间轴之后的独立大块。
15. **🔴 P2 质量门控强制校验**（详见 `knowledge/model_rules/quality_gate_checklist.md`）：视频提示词输出前必须逐条过 6 条门控：
    - 门控1 结构完整性（7段齐全）
    - 门控2 多模态引用（编号连续/用途明确/数量合规）
    - 门控3 镜头语言（标准写法/不堆叠/前后连贯）
    - 门控4 一致性锁定（外观锁定+每镜重复+画质参数具体）
    - 门控5 商业合规（无真实品牌/角色/血腥/敏感）
    - 门控6 内聚化格式（声音内嵌+转场显性+格式统一）
    任意一条不通过必须修正后重新检查，全部通过才能交付。
16. 输出可直接投喂模型的视频提示词 + @媒体引用 + 出片设置 + **质量门控自检表**。

### 两种 Prompt 的区分（不可混淆）

| 类型 | 用途 | 核心要素 | 不包含 |
|---|---|---|---|
| **九宫格生图 Prompt** | Gemini / Nano Banana 生成分镜图 | 景别+构图+主体+动态+场景+风格 | 时间轴、运镜、转场、对白、音效 |
| **Seedance 视频 Prompt** | Seedance 生成视频 | 生图要素 + 时间轴+运镜+转场+动作链+连续性+对白+音效 | — |

> 禁止把两种 Prompt 混成一个。生图 Prompt 只负责画面，视频 Prompt 才负责时间与运动。

## /优化 流程

对已有 Prompt 做局部修正：指哪改哪，默认"其余不变"，不推倒重来。支持改时长/改场景/改元素/改对白/改风格。

## 硬性原则

- **🔴 最高优先级**：`/生成` 必须分阶段推进，禁止跳过分镜确认直接给最终 Prompt（见 `/生成 流程`）。
- 全程使用用户语言；台词用目标市场语言并附中文释义。
- **产品外观/颜色/logo 以用户图为唯一事实**，提示词不得改设计（见 model_rules/product_consistency.md）。
- 跨境与国内打法不混用：用户做哪个市场就用哪套表达与平台规则。
- **具体工具名/入口不写死**（模型迭代快），只保留"简单品用轻量模型、高难度镜头用旗舰模型"的分层策略。
- 每阶段产物可直接复制使用，不给半成品骨架。
- **🔴 中英双文强制输出**：所有提示词（分镜图提示词 + 视频提示词）必须输出中英双文版，先中文后英文，**分开输出不混排**，每版独立完整块方便直接复制；英文版严格过语言一致性门控（正文无中文字符残留，段落标题可保留中文标记如【Style】）。
- 先验证再放量：先出 2-3 条测方向，数据可行再批量。
